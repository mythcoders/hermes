<strong class="text-red"><%= pluralize(@errors.count, "error") %> prohibited your submission from being saved:</strong>
<ul class="text-red">
  <% @errors.each do |error| %>
    <li><%= error %></li>
  <% end %>
</ul>
