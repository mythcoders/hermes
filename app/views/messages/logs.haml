- page_title 'View Message', 'Mail Log'

= render partial: 'header', locals: { action: :logs }

.card.p-0
  %table-responsive
    %table.table.table-sm
      %thead
        %tr
          %th Event
          %th Details
          %th.float-right Timestamp
      %tbody
        - @logs.each do |r|
          %tr{style: "border-bottom: none"}
            %td
              = r.activity_type
            %td.text-monospace
              - if r.activity_type == "clicked"
                = r.link_url
                %hr
              = r.user_agent
            %td.float-right= r.notification_timestamp
          -# %tr{title: r.notification_timestamp}
          -#   %td{colspan: 3}
          -#     .text-monospace
          -#       - if r.activity_type == "delivered"
          -#         = r.reporting_mta
          -#         %br= r.smtp_response
          -#       - else
          -#         = r.user_agent
